#!/usr/bin/env python3
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]


def replace_once(path, old, new):
    file_path = ROOT / path
    data = file_path.read_text()
    if new in data:
        return
    count = data.count(old)
    if count != 1:
        raise RuntimeError(f"{path}: expected one replacement, found {count}")
    file_path.write_text(data.replace(old, new, 1))


def append_text(path, marker, text):
    file_path = ROOT / path
    data = file_path.read_text()
    if marker in data:
        return
    file_path.write_text(data.rstrip() + "\n\n" + text.lstrip())

# Configuration model and validation.
replace_once("internal/config/config.go", 'type Config struct {\n\tDefaultProfile string             `yaml:"default_profile"`\n\tProfiles       map[string]Profile `yaml:"profiles"`\n\tPoints         []Point            `yaml:"points,omitempty"`\n}\n', 'type Config struct {\n\tDefaultProfile string             `yaml:"default_profile"`\n\tProfiles       map[string]Profile `yaml:"profiles"`\n\tPoints         []Point            `yaml:"points,omitempty"`\n\tGroups         []PointGroup       `yaml:"groups,omitempty"`\n}\n')
replace_once("internal/config/config.go", 'type Point struct {\n\tName        string `yaml:"name" json:"name"`\n\tTag         string `yaml:"tag" json:"tag"`\n\tType        string `yaml:"type,omitempty" json:"type"`\n\tElements    uint16 `yaml:"elements,omitempty" json:"elements"`\n\tUnit        string `yaml:"unit,omitempty" json:"unit,omitempty"`\n\tWritable    bool   `yaml:"writable,omitempty" json:"writable"`\n\tDescription string `yaml:"description,omitempty" json:"description,omitempty"`\n}\n', 'type Point struct {\n\tName        string `yaml:"name" json:"name"`\n\tTag         string `yaml:"tag" json:"tag"`\n\tType        string `yaml:"type,omitempty" json:"type"`\n\tElements    uint16 `yaml:"elements,omitempty" json:"elements"`\n\tUnit        string `yaml:"unit,omitempty" json:"unit,omitempty"`\n\tWritable    bool   `yaml:"writable,omitempty" json:"writable"`\n\tDescription string `yaml:"description,omitempty" json:"description,omitempty"`\n}\n\ntype PointGroup struct {\n\tName        string   `yaml:"name" json:"name"`\n\tPoints      []string `yaml:"points" json:"points"`\n\tDescription string   `yaml:"description,omitempty" json:"description,omitempty"`\n}\n')
replace_once("internal/config/config.go", '\t\tPoints: []Point{\n\t\t\t{Name: "motor_speed", Tag: "Motor.Speed", Type: "real", Elements: 1, Unit: "rpm"},\n\t\t\t{Name: "motor_enabled", Tag: "Motor.Enable", Type: "bool", Elements: 1, Writable: true},\n\t\t},\n\t}\n}\n', '\t\tPoints: []Point{\n\t\t\t{Name: "motor_speed", Tag: "Motor.Speed", Type: "real", Elements: 1, Unit: "rpm"},\n\t\t\t{Name: "motor_enabled", Tag: "Motor.Enable", Type: "bool", Elements: 1, Writable: true},\n\t\t},\n\t\tGroups: []PointGroup{\n\t\t\t{Name: "motor", Points: []string{"motor_speed", "motor_enabled"}, Description: "Motor operating values"},\n\t\t},\n\t}\n}\n')
replace_once("internal/config/config.go", '\tseen := map[string]struct{}{}\n\tfor _, declared := range c.Points {\n\t\tpoint := normalisePoint(declared)\n\t\tif point.Name == "" {\n\t\t\treturn fmt.Errorf("%w: point name is required", ErrConfig)\n\t\t}\n\t\tkey := strings.ToLower(point.Name)\n\t\tif _, ok := seen[key]; ok {\n\t\t\treturn fmt.Errorf("%w: duplicate point name %q", ErrConfig, point.Name)\n\t\t}\n\t\tseen[key] = struct{}{}\n\t\tif point.Tag == "" {\n\t\t\treturn fmt.Errorf("%w: point %q tag is required", ErrConfig, point.Name)\n\t\t}\n\t\tif !validPointType(point.Type) {\n\t\t\treturn fmt.Errorf("%w: point %q has unsupported type %q", ErrConfig, point.Name, point.Type)\n\t\t}\n\t\tif point.Elements == 0 {\n\t\t\treturn fmt.Errorf("%w: point %q elements must be between 1 and 65535", ErrConfig, point.Name)\n\t\t}\n\t\tif point.Writable && point.Type == "auto" {\n\t\t\treturn fmt.Errorf("%w: writable point %q requires an explicit type", ErrConfig, point.Name)\n\t\t}\n\t}\n\treturn nil\n}\n', '\tseen := map[string]struct{}{}\n\tfor _, declared := range c.Points {\n\t\tpoint := normalisePoint(declared)\n\t\tif point.Name == "" {\n\t\t\treturn fmt.Errorf("%w: point name is required", ErrConfig)\n\t\t}\n\t\tkey := strings.ToLower(point.Name)\n\t\tif _, ok := seen[key]; ok {\n\t\t\treturn fmt.Errorf("%w: duplicate point name %q", ErrConfig, point.Name)\n\t\t}\n\t\tseen[key] = struct{}{}\n\t\tif point.Tag == "" {\n\t\t\treturn fmt.Errorf("%w: point %q tag is required", ErrConfig, point.Name)\n\t\t}\n\t\tif !validPointType(point.Type) {\n\t\t\treturn fmt.Errorf("%w: point %q has unsupported type %q", ErrConfig, point.Name, point.Type)\n\t\t}\n\t\tif point.Elements == 0 {\n\t\t\treturn fmt.Errorf("%w: point %q elements must be between 1 and 65535", ErrConfig, point.Name)\n\t\t}\n\t\tif point.Writable && point.Type == "auto" {\n\t\t\treturn fmt.Errorf("%w: writable point %q requires an explicit type", ErrConfig, point.Name)\n\t\t}\n\t}\n\tseenGroups := map[string]struct{}{}\n\tfor _, declared := range c.Groups {\n\t\tgroup := normaliseGroup(declared)\n\t\tif group.Name == "" {\n\t\t\treturn fmt.Errorf("%w: group name is required", ErrConfig)\n\t\t}\n\t\tkey := strings.ToLower(group.Name)\n\t\tif _, ok := seenGroups[key]; ok {\n\t\t\treturn fmt.Errorf("%w: duplicate group name %q", ErrConfig, group.Name)\n\t\t}\n\t\tseenGroups[key] = struct{}{}\n\t\tif len(group.Points) == 0 {\n\t\t\treturn fmt.Errorf("%w: group %q requires at least one point", ErrConfig, group.Name)\n\t\t}\n\t\tseenPoints := map[string]struct{}{}\n\t\tfor _, pointName := range group.Points {\n\t\t\tpointKey := strings.ToLower(pointName)\n\t\t\tif pointName == "" {\n\t\t\t\treturn fmt.Errorf("%w: group %q contains an empty point name", ErrConfig, group.Name)\n\t\t\t}\n\t\t\tif _, ok := seenPoints[pointKey]; ok {\n\t\t\t\treturn fmt.Errorf("%w: group %q contains point %q more than once", ErrConfig, group.Name, pointName)\n\t\t\t}\n\t\t\tseenPoints[pointKey] = struct{}{}\n\t\t\tif _, err := c.Point(pointName); err != nil {\n\t\t\t\treturn fmt.Errorf("%w: group %q references unknown point %q", ErrConfig, group.Name, pointName)\n\t\t\t}\n\t\t}\n\t}\n\treturn nil\n}\n')
replace_once("internal/config/config.go", 'func (c Config) NormalisedPoints() []Point {\n\tpoints := make([]Point, len(c.Points))\n\tfor i, point := range c.Points {\n\t\tpoints[i] = normalisePoint(point)\n\t}\n\treturn points\n}\n\nfunc Load(path string) (Config, error) {\n', 'func (c Config) NormalisedPoints() []Point {\n\tpoints := make([]Point, len(c.Points))\n\tfor i, point := range c.Points {\n\t\tpoints[i] = normalisePoint(point)\n\t}\n\treturn points\n}\n\nfunc (c Config) Group(name string) (PointGroup, error) {\n\tname = strings.TrimSpace(name)\n\tfor _, declared := range c.Groups {\n\t\tgroup := normaliseGroup(declared)\n\t\tif strings.EqualFold(group.Name, name) {\n\t\t\treturn group, nil\n\t\t}\n\t}\n\treturn PointGroup{}, fmt.Errorf("%w: group %q does not exist", ErrConfig, name)\n}\n\nfunc (c Config) NormalisedGroups() []PointGroup {\n\tgroups := make([]PointGroup, len(c.Groups))\n\tfor i, group := range c.Groups {\n\t\tgroups[i] = normaliseGroup(group)\n\t}\n\treturn groups\n}\n\nfunc (c Config) PointsForGroup(name string) (PointGroup, []Point, error) {\n\tgroup, err := c.Group(name)\n\tif err != nil {\n\t\treturn PointGroup{}, nil, err\n\t}\n\tpoints := make([]Point, 0, len(group.Points))\n\tcanonicalNames := make([]string, 0, len(group.Points))\n\tfor _, pointName := range group.Points {\n\t\tpoint, err := c.Point(pointName)\n\t\tif err != nil {\n\t\t\treturn PointGroup{}, nil, err\n\t\t}\n\t\tpoints = append(points, point)\n\t\tcanonicalNames = append(canonicalNames, point.Name)\n\t}\n\tgroup.Points = canonicalNames\n\treturn group, points, nil\n}\n\nfunc Load(path string) (Config, error) {\n')
replace_once("internal/config/config.go", 'func normalisePoint(point Point) Point {\n\tpoint.Name = strings.TrimSpace(point.Name)\n\tpoint.Tag = strings.TrimSpace(point.Tag)\n\tpoint.Type = strings.ToLower(strings.TrimSpace(point.Type))\n\tif point.Type == "" {\n\t\tpoint.Type = "auto"\n\t}\n\tif point.Elements == 0 {\n\t\tpoint.Elements = 1\n\t}\n\tpoint.Unit = strings.TrimSpace(point.Unit)\n\tpoint.Description = strings.TrimSpace(point.Description)\n\treturn point\n}\n\nfunc validPointType(value string) bool {\n', 'func normalisePoint(point Point) Point {\n\tpoint.Name = strings.TrimSpace(point.Name)\n\tpoint.Tag = strings.TrimSpace(point.Tag)\n\tpoint.Type = strings.ToLower(strings.TrimSpace(point.Type))\n\tif point.Type == "" {\n\t\tpoint.Type = "auto"\n\t}\n\tif point.Elements == 0 {\n\t\tpoint.Elements = 1\n\t}\n\tpoint.Unit = strings.TrimSpace(point.Unit)\n\tpoint.Description = strings.TrimSpace(point.Description)\n\treturn point\n}\n\nfunc normaliseGroup(group PointGroup) PointGroup {\n\tgroup.Name = strings.TrimSpace(group.Name)\n\tgroup.Description = strings.TrimSpace(group.Description)\n\tpoints := make([]string, len(group.Points))\n\tfor i, point := range group.Points {\n\t\tpoints[i] = strings.TrimSpace(point)\n\t}\n\tgroup.Points = points\n\treturn group\n}\n\nfunc validPointType(value string) bool {\n')
# Starter example.
append_text("internal/config/config_test.go", "TestGroupLookupAndPointResolution", 'func TestGroupLookupAndPointResolution(t *testing.T) {\n\tcfg := Starter()\n\tgroup, points, err := cfg.PointsForGroup("MOTOR")\n\tif err != nil {\n\t\tt.Fatal(err)\n\t}\n\tif group.Name != "motor" || len(points) != 2 || points[0].Name != "motor_speed" {\n\t\tt.Fatalf("group=%#v points=%#v", group, points)\n\t}\n}\n\nfunc TestRejectUnknownPointInGroup(t *testing.T) {\n\tcfg := Starter()\n\tcfg.Groups = []PointGroup{{Name: "bad", Points: []string{"missing"}}}\n\tif err := cfg.Validate(); err == nil {\n\t\tt.Fatal("expected unknown point error")\n\t}\n}\n\nfunc TestRejectDuplicateGroupNamesCaseInsensitively(t *testing.T) {\n\tcfg := Starter()\n\tcfg.Groups = append(cfg.Groups, PointGroup{Name: "MOTOR", Points: []string{"motor_speed"}})\n\tif err := cfg.Validate(); err == nil {\n\t\tt.Fatal("expected duplicate group error")\n\t}\n}\n')
